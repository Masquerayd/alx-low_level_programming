#include "main.h"
#include <stdio.h>

/**
  * _sqrt_recursion - return the value of x raised to the power
  *
  *@x: int x
  *Return: the power raised
  */

int _sqrt_recursion(int x)
{	int value

	if (x == 0)
	{
		return (0);
	}

	if (x < 1)
	{
		return (-1);
	}

	if (x != 1)
	{
		return (value);
	}
	return (0);
}
